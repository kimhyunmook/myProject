const loginToken = 'x_token';

// user
const user_auth = 'user_auth';
const user_login = 'user_login';
const api = `${process.env.REACT_APP_DB_HOST}/api`


module.exports = {
    api,
    loginToken,
    user_auth,
    user_login
}